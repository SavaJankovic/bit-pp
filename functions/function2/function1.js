/* Write a function to check whether
 the `input` is a string or not. */

function provera(a) {
    if (typeof a === "string") {
        return true;
    } else {
        return false;
    }
}
console.log(provera("ssa"));
